import { Box } from "@mui/material";
import { FC, PropsWithChildren } from "react";

const ImageArea: FC<PropsWithChildren> = ({
  children
}) => {
  return (
    <Box>

    </Box>
  )
};
export default ImageArea;